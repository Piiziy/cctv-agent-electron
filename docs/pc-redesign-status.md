# PC 앱 리디자인 — 디자인과 다른 점 · 검증 현황

`feat/scene-stealer-pc-redesign` 기준. 디자인 출처는 `design/씬스틸러 PC 앱.dc.html`(화면)과
`design/씬스틸러 디자인시스템.dc.html`(토큰·컴포넌트).

`handoff/CLAUDE-CODE-프롬프트.md` 의 완료 기준은 "디자인 HTML과 나란히 비교한 차이점 목록"이다.
아래 목록은 **코드에서 의도적으로 다르게 한 것**이고, **화면을 브라우저에 띄워 나란히 놓고 본
비교는 아직 하지 않았다** (맨 아래 참고).

---

## 1. 일부러 다르게 한 것

모두 같은 원칙에서 나왔다 — **아직 사실이 아닌 것을 화면에 띄우지 않는다.**
없는 기능을 약속하는 버튼·문구는 사장님이 그걸 믿고 행동하게 만든다.

| 화면 | 디자인 | 구현 | 이유 |
|---|---|---|---|
| 2a | QR 페어링 줄 | 뺐다 | 요구사항 1.5 가 백엔드 범위 밖. 찍어도 아무 일이 없다 |
| 2a | 다른 PC 매장 선택 = 바로 교체 | 교체 경고 한 줄 추가 | 요구사항 1.4 '교체 확인'. 교체하면 그 PC 의 감시가 멈춘다 |
| 셸 | `홍길동님` | 가린 휴대폰 번호 `010-****-5678` | 로그인이 휴대폰 번호뿐이라 이름이 없다 |
| 2b | 미리보기 `2초마다 갱신` | `실시간 미리보기` | 지금은 스냅샷이 아니라 실시간 스트림이다 |
| 2b | 위치 태그 5개 | `기타` 추가 | 요구사항 2.1 의 태그 목록에 있다 |
| 2c | '하나 크게' 펼친 화면 없음 | 큰 카드 1 + 아래 한 줄 | 디자인에 그려진 화면이 없어 같은 카드로 조합 |
| 2d | `모바일 앱에도 푸시 전송됨 ✓` | 뺐다 | PC 는 푸시가 나갔는지 알 수 없다(SSE 에 결과 없음, FCM 키도 아직 없음) |
| 2d | `확인하지 않으면 5분 후 모바일에 1회 재알림` | `✕ 닫기 = 미확인 유지` 만 | 재알림(6.5)은 백엔드 2순위라 아직 동작하지 않는다 |
| 2d · 2f | 112 안내문에 `클립 링크` | 링크 없음 | 공유 링크(5.2)가 없고, 서명 URL 은 1시간이면 만료된다 |
| 2f | `증거 묶음 내보내기` (1차 버튼) | 비활성 + '준비 중' 안내 | 요구사항 5.3 이 후순위라 백엔드에 없다 |
| 2f | 드래그로 내보낼 범위 선택 | 없음 | 쓸 곳(증거 묶음)이 없다 |
| 2f | `클립만 저장 · 공유 링크 (7일)` | `클립만 저장` | 공유 링크(5.2)가 없다 |
| 2f | 플레이어 `📷 스냅샷` | 없음 | 요구사항 목록에 없고, 저장 경로를 따로 열어야 한다 |
| 2f | 조각 타임라인 옆 카메라 강조(`출입문 · 퇴장 14:32:38`) | 강조 없음 | 동시각 카메라 응답에 그 카메라의 이벤트 정보가 없다 |
| 2g | `모바일 푸시 · 켬 · iPhone 연결됨` | 연결 방법 안내 | 푸시 기기 조회 API 가 없다 |
| 2g | '알림' 칸만 그려져 있음 | 나머지 칸(운영 시간·모바일·카메라 관리·저장 공간·매장 정보)도 만듦 | 같은 카드·줄 모양으로 조합 |

## 2. 디자인에 없던 상태를 고른 것

값은 전부 디자인시스템 팔레트 안에서 골랐고, 코드에 이유를 한 줄씩 남겼다.

- **버튼 hover/active** — 같은 색 계열의 다음 단계(blue/700·800, error/700·800, gray/50·100).
  navy(`#14233D`)는 더 어두운 단계가 없어 밝기로 표현했다.
- **disabled** — 디자인의 `loading = gray/300` 과 같은 모양. 둘 다 "지금 누를 수 없다"는 뜻이다.
- **'서버 연결 끊김' 배너** — error/50 바탕 + error/main 글자 (위험 카드 배경과 같은 계열).
- **매장 선택 칩 드롭다운** — 카드 반경 + modal 그림자.
- **2e 표의 '미확인' 태그** — 디자인대로 빨간 계열. 디자인시스템 샘플의 파란 '미확인'과 다르다.

## 3. 실제로 동작을 바꾼 것 (디자인 밖)

- **2d 팝업은 위험도 '높음'과 응급(쓰러짐)에만 뜬다.** 보통·낮음은 피드와 배지로 쌓인다.
  계산대 PC 앞에서 '장시간 배회'마다 창이 튀어나오면 사장님이 알림을 무시하게 된다.
  2g 'PC 팝업 + 소리'로 끌 수 있다.
- **기본 알림 빠르기(조각 길이) 5분 → 1분.** 디자인 2b 의 기본값이자 백엔드 기본값.
  이미 설치된 PC 의 저장값은 바뀌지 않는다.
- **2d 경보음은 음원 파일 없이 WebAudio 로 만든다.**

---

## 4. 검증 현황

| | 결과 |
|---|---|
| 타입 검사 | ✅ `npm run typecheck` 통과 |
| 단위 테스트 | ✅ 241개 통과 (기존 164 + SSE 파서·하트비트·로그인 세션·API 프록시·SSE 재연결·미리보기 설정·112 안내문) |
| 빌드 | ✅ `electron-vite build` (main·preload·renderer) 성공, 빌드 기본값이 main 번들에 박히는 것까지 확인 |
| 컴파일된 CSS | ✅ 피그마 토큰·추가 토큰·컴포넌트 클래스·폰트가 실제로 나오는지 확인. 그 과정에서 `shadow-card` 가 그림자 '색' 유틸리티와 이름이 겹쳐 카드 그림자가 덮이던 버그를 잡았다 |
| **디자인 HTML 과 화면 나란히 비교** | ❌ **아직 안 했다.** 브라우저 프리뷰가 워크트리가 아닌 메인 레포에서 떠서, 세션 디렉터리를 옮기는 중이다 |
| Electron 실행 · 실제 카메라 | ❌ 안 했다 |
| 실제 백엔드 · Supabase 연동 | ❌ 안 했다 (프로젝트 없음). 전부 `docs/api-contract.md` 와 mock 으로만 맞췄다 |
